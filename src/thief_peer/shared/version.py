"""Code version identifier (PRD_6 §2.5, §3): included in every Step-0
declaration so an auditor can tell which version of the code played a
given match, alongside the exact git commit hash (`peer/sealing.py`)."""

CODE_VERSION = "1.00"
