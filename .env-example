# Copy this file to .env and fill in real values. .env is gitignored.
# None of these are needed yet for Stage 1 (base logic, no network/secrets).
# Populated as later stages need them:
#   Stage 4 — LLM provider keys (ANTHROPIC_API_KEY, if using claude_api mode)
#   Stage 7 — Gmail API OAuth (credentials.json / token.json, not env vars —
#             see docs/PRD_7_reporting_shell.md and Appendix א of the book)

ANTHROPIC_API_KEY=sk-ant-dummy-placeholder-not-a-real-key
